---
import DefaultLayout from "../layouts/DefaultLayout.astro";

const impressumData = await Astro.glob("../content/impressum/*.md");
const impressum = impressumData[0];
---

<DefaultLayout title="Impressum">
  <impressum.Content />
</DefaultLayout>
